{
  "name": "functions",
  "version": "0.0.1",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "concurrently \"yarn run build:watch\" \"firebase serve --only functions\"",
    "predeploy": "yarn run build",
    "deploy": "firebase deploy --only functions"
  },
  "main": "dist/index.js",
  "dependencies": {
    "firebase-admin": "^8.6.0",
    "firebase-functions": "^3.1.0",
    "public-holidays": "^0.2.2"
  },
  "devDependencies": {
    "concurrently": "^4.1.2",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  },
  "private": true
}
